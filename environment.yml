name: anospp_analysis_dev
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  # non-python requirements
  - vsearch
  # python requirements should go to pyproject.toml
  # this section only deals with basic poetry installation
  - python=3.10.*
  - pip
  - pip:
    - "poetry>=1.2"